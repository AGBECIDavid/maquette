/**
 * Version applicative.
 *
 * Source unique : c'est ce numéro que l'application affiche et qu'un
 * bêta-testeur citera dans son signalement. Le tenir à jour dans
 * `package.json` en même temps — un bug rapporté sans version est un bug
 * qu'on cherche dans le mauvais code.
 */
export const APP_VERSION = '0.4.0';
export const APP_STAGE = 'bêta';
